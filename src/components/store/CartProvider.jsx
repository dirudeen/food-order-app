import React from 'react';
import CartContext from './cart-context';

const CartProvider = () => {
    const values = {
        item: [],
        totalAmount: 0,
        
    }
    return (
        <CartContext>
        
        </CartContext>
    );
}

export default CartProvider;
